from DataType.Asset.AbstractAsset import Asset as AssetObject, AssetFolder


def generate_object(asset:AssetObject) -> any:
    """
    Instantiate asset objects to an instances and return it
    """

def match_object(sub_path: str) -> AssetObject or AssetFolder:
    """
    Match asset object by sub_path
    """

